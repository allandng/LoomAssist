// Phase 5 (settings modal) — full implementation
export function SettingsPage() {
  return <div style={{ flex: 1, display: 'flex', alignItems: 'center', justifyContent: 'center', color: 'var(--text-muted)' }}>Settings (Phase 5)</div>;
}

export function SettingsSidebarContent() {
  return null;
}
